# My HERO — 開発ルールブック（Claude Code 用・唯一の正本）

これは iPhone Safari / Instagram 内蔵ブラウザ(WKWebView)で動く**単一HTMLファイルの子ども向け2D横スクロールアクション**の開発ルール。
一般論のテンプレではなく、**v210〜v236 の実開発ログから帰納したルール集**。各ルールの【根拠】は実際に起きた事故。
仕様・現状・タスクは `docs/SPEC.md`。このファイルは「どう作業するか」だけを扱う。

**運用原則：新しい失敗が起きたら、修正だけで終わらせず「ルール＋根拠」を本ファイルに1行追加する。**

---

## 0. 大原則

1. **「動くはず」を出荷しない。** コミットできるのは「検証済み」か「未検証と明記＋実機チェックリストを `docs/SPEC.md` §未検証 に追記済み」のどちらかのみ。
2. **1コミット＝1テーマ。** 変更は小さく、必ず可逆に。バージョンを進めるときは `vNNN` でタグを打つ。
3. **実機が唯一の真実。** iPhone Safari / IG内蔵WKWebView での見え方が正。開発環境の見た目・理屈・ヘッドレスの結果より実機スクショを優先する。実機確認はユーザー（カマタク）にしかできないので、確認依頼は「どの画面で・何を・どうなっていればOKか」を具体的に書く。
4. **行番号をドキュメント・コメント・報告に書かない。** 参照はすべて grep で一発で引ける関数名・変数名・文字列（例:「`_ccRemoveWhiteBg` の四隅サンプル」）。
   【根拠】v234c: 行番号参照122箇所を全数照合したら本文18箇所がズレていた。出荷前ゲートの `ctx.ellipse` 位置が文書内で矛盾し、誤判定寸前だった。
5. **同じ情報の二重管理をしない。** ルールは本ファイル、仕様は SPEC.md、コードの真実はコードのみ。転記が必要になったら設計を疑う。
6. **ユーザーとの会話は関西弁まじりのカジュアルで、ただし技術判断は一切ゆるめない。** 提案には必ずコスト・リスク・未検証範囲を明記する。

## 1. 実装前ゲート（書き始める前に数値を出す）

1. **配置計算**：新しいUI要素は、実機ビューポート（§7）に対する最終位置・サイズをpxで算出し、既存要素との重なりを机上で確認してから書く。
   【根拠】v211: ロゴの高さ(585px)に押されて新要素が画面外へ。
2. **知覚検算**：アニメは振幅と周期から速度px/sを出してから書く。持続的な位置アニメは **3px/s 未満にしない**（人間には静止に見える）。環境系（雲など）は20px/s超だと騒がしい。
   【根拠】v217: 0.8px/s の雲 →「動いてない」判定。v209: 2.7px/s のロゴ →「変わらん」判定。同型の失敗2回。
3. **識別子衝突チェック**：新しい id / class / 関数名 / @keyframes 名は `grep -c '名前' game/my_hero.html | grep -iv base64 相当` で既存0件を確認してから使う。
   **デザインシステムの修飾クラス（`.uiBtn.go` の `go` など）も「新しい名前」として数える。**
   単独クラスの既存ルール（`#overlay .go`）が特異度で勝ち、要素ごと壊すことがある。
   衝突が避けられないときは既存側を `:not(.uiBtn)` で狭める。
   【根拠】v212: `#startBtn` 衝突。v220: `starPop` 衝突。
   v292: `.uiBtn.go`（緑ボタン）が点滅ヒント用の `#overlay .go{animation:bl}` に負けて
   **ボタンごと点滅して消えていた**。v287の「これでOK」が実際に半分の時間消えており、
   ヘッドレスのスクショでも「ボタンが無い」ように見えて原因究明に時間を使った。
4. **テイスト整合**：このゲームのアートはAI生成のリッチな厚塗り調。フラットなベクター絵はロゴと並ぶと安っぽく見える。手描き系の新規ビジュアルは実機文脈プレビュー（実際の背景・隣接要素と合成した静止画）で承認を得てから統合する。
   【根拠】v213: 単体では良く見えたシルエット勇者が実機で「ダサい」判定 → 全撤去。
5. **効果が見える面積を先に実測する。** 変える対象が画面の何%かを測ってから実装する。占有率が小さいなら施策自体が成立しない。
   【根拠】v224: 背景グラデ変更の可視域が実測24px（画面の3.5%）しかなかった。
6. **blend-mode / mask-image / filter はWKWebViewで高コスト。** 使うなら fps 実測A/Bを実装前ゲートに含める。
   【根拠】v226: `mix-blend-mode:multiply` 1枚でホームが 60→42.8fps。
7. **描画コール予算**：新しい canvas 描画は、既存最重量パターン（`bgGrass` ≒ 121〜256コール）を上限とし、実装後 `node tools/render_bg.js` でコール数と NaN を計測してからコミットする。同色の図形は1つの `beginPath` に複数サブパスでまとめる。**1パスに複数図形を入れるときは各図形の前に `moveTo` を置く**（省くと arc 同士が直線で結ばれて図形が化ける。v236の噴煙で実際に発生）。
   【根拠】v234: `bgSnow` 素直に描いたら579コール＝最重量の2.3倍。パス統合で254まで削減。
8. **難易度・配置の変更は、実装前に新旧の実効構成をNodeで算出して表にする。** ハーネス自体が既知の実測値を再現できることを先に照合し、再現できて初めて出力を信用する。
   【根拠】v232: ハーネスがv231実測表を3/3再現できたので、その出力を検証済みとして扱えた。
9. **遠景ランドマークの視差は「浅く＋クランプ」**。ラップ方式(`%周期`)にするとステージ後半で画面外に出たまま戻らないことがある。光源・煙などの付随要素は必ず本体と同じ基準座標から描く。
   【根拠】v236: 火山だけが流れて火口グローと噴煙が置き去りになった。
10. **バイオームに合わせたキャラ絵は「背景と同化」を数値で確認してから入れる。**
   ⚠️ **測り方は §1-15 が正（外周2px比）。以下の「平均色で測る」は誤りだったので使わない。**
   輪郭を焼き込む判断も §1-16 が優先（外側に足さない）。
   【根拠】v240: 雪山10体を入れたら10体中7体が基準割れ、ツラランリンは1.02＝ほぼ不可視だった。
   「雰囲気が合う」ことと「見える」ことは別問題。
11. **どうしても輪郭が要るときだけ、色は背景の明暗で選ぶ。濃い輪郭を固定で使わない。**
   ⚠️ **まず §1-15 で測り直すこと。v249 の実測では13体すべて輪郭は不要だった。**
   足す場合も §1-16 のとおり内側に。背景の平均輝度を出し、明るい背景には濃い色、暗い背景には明るい色。
   【根拠】v244: v240の濃紺 `#262A3A` を暗い夕暮れ背景(輝度0.09)のW2に使ったら、
   輪郭なし1.02〜2.46 → 輪郭あり1.10〜1.83 と**足すほど悪化**した。
   生成り `#FFF3D0` に替えたら1.51〜3.09に改善。「v240で効いた手」を検算せず転用しかけた。
12. **新しいまものを入れる前に、そのワールドで実際に出る敵タイプを `groundPool`／`flyPool` で確認する。**
   ゲートに引っかかるタイプの絵は永久に画面へ出ない。
   【根拠】v244: W2は `normal`＋`giant` のみ（fast=W3〜／shooter,fire=W4〜／飛行=W5〜）。
   図鑑どおり10体入れると7体が死蔵データになるところだった。
13. **埋め込むPNGは256色に減色してから入れる。** `quantize(colors=256, method=FASTOCTREE)`。
   ただし **効くのは「縮小して表示する絵」だけ**。適用の可否は表示サイズで決める。
   - **小さく描く絵（まもの22〜37px・原画50〜100px）→ 適用する。** 実表示での色差は最大1.1%、
     外周2px輝度（§1-15の指標）の変化は最大0.0064。目視で見分けがつかない。
   - **等倍に近い絵（キャラ選択の顔66px・原画48〜74px）→ 適用しない。** 実表示でバンディングと
     色ムラが見える（黒髪がザラつく／髪に紫のムラ）。
   【根拠】v245: W3の7体 52,140B→38,563B（26%減）。v291: まもの41個 361KB→134KB（63%減）で
   ファイル全体 2,105,532B→1,788,568B（-15%）。同じ処理でも顔8個は劣化が見えたので除外した。

13b. **base64の棚卸しは `data:image/...;base64,` だけで数えない。**
   接頭辞を持たない**裸のbase64**（`iVBORw0KGgo` / `/9j/` で始まる文字列を配列に入れ、
   使う側で接頭辞を連結する形）があると、そちらが丸ごと集計から漏れる。
   【根拠】v291: 最初の集計で画像は58%と出たが、まもの35個（361KB）が漏れていて実際は75%だった。
   漏れた側の方が削減効果が大きかった。**必ず両方の形式で走査する。**
14. **「危なそう」を先入観で決めない。バイオームの危険度は必ず輝度で測る。**
   背景が暗いワールドほど、たいていのまものは見える。危ないのは**背景が明るい**ワールド。
   【根拠】v245: 洞窟を「灰色の岩が沈む＝🔴最悪」と予想していたが、実測は7体中0体が基準割れ。
   一方で明るい雪山(輝度0.59)は7/10体が割れた。色名のイメージで判断していたのが誤り。

15. **「見えるか」は平均色ではなく輪郭部の局所コントラストで測る。**
   §1-10〜§1-13 で使っていた「まもの全体の平均色 vs 背景の平均色」は**指標が間違っていた**。
   シルエットが読めるかを決めるのは**外周2pxと背景の比**であって、絵全体の平均ではない。
   平均で測ると「埋もれている」と誤判定し、いらない輪郭を足してしまう。
   【根拠】v249: W2・W3の13体は平均比だと輪郭なしで1.00〜5.63（W2は基準割れ）だが、
   外周2pxで測ると **1.98〜9.21 で全13体が基準クリア**。実際に合成して見ても問題なく見える。
   逆に焼き込んだ生成り輪郭は外周比 6.20〜14.67 と**強すぎ**て、それがシール感の正体だった。
   **輪郭を足す前に、まず外周比で測り直す。たいていの場合そもそも足す必要がない。**

16. **外側に足した輪郭は「シール貼った感」になる。足すなら内側に。**
   絵のアルファを膨張させて外周に色を足す方式は、縮小表示されると白フチのコラージュに見える。
   実装コストが同じなら、**元のシルエットを変えない**方法（内側のリム／接地影／背景側を沈める）を先に試す。
   【根拠】v244・v245で13体に生成り輪郭を焼き込み、実機で「切り取りが不自然すぎて急に安っぽい」
   と指摘された。剥がしたら見た目もファイルサイズ（-20%）も改善した。

18. **デザイン画像は「背景だけ版」をもらう。実行時に変わるものを絵に焼き込ませない。**
   数字・星・ロック・選択枠・キャラ・ナビバー・TESTバッジ・HUD は**画像に描かせない**。
   焼き込むと嘘の値が出るか、実物と二重に表示される。発注文は `docs/DESIGN_PROMPT.md`。
   【根拠】v284〜v286 で、もらった絵から「戻る/スタート」「0 / 5 体」「TESTバッジ」
   「ナビバー」を毎回塗り消した。周囲を引き伸ばして埋めるので**拡大すると塗り跡が残る**。
   最初から描かれていなければこの劣化はゼロ。

17. **加工した画像を入れるコミットでは、加工前の画像も同時に履歴へ残す。**
   §4-1 は「加工前が履歴にある」ことを前提にしているが、**新規追加と加工を同じコミットでやると
   加工前が履歴に一度も存在しない**。v244/v245がこれで、元絵に戻す手段が無かった。
   今回は輪郭が外側リングだったのでアルファ収縮で復元できたが、幸運に頼っただけ。
   【根拠】v249: 元絵を探したが git にもリポジトリにも無く（画像ファイル0件・全部base64）、
   復元可能性を実測で確認してから作業する羽目になった。

## 2. 実装中の規約

1. 編集は `str_replace` 相当の一意置換、または**置換回数を assert で検証するパッチ**（`assert s.count(old)==1`）で行う。曖昧な一括置換をしない。
2. **二重マークアップ同期**：ホーム画面は静的HTML側（`#overlay` 直下）とJS注入側（`showTitleScreen`）の2箇所にある。片方を変えたら必ずもう片方も。JS側で動的生成して両方に効かせられるならその方が安全（`.home-tint` 方式）。
3. **canvas規約**：`save()`/`restore()` は必ず対。`fill()`/`stroke()` はスタイル設定と同じ save 区間内で完結。
   【根拠】v213: restore() 後の fill() で接地影が真っ黒に。
4. **アニメは transform / opacity のみ**。追加DOMは最小限、`will-change:transform` を付ける。
5. **禁止API**：`ctx.ellipse()` は使わず `ctxEllipse()` ヘルパー。`ctx.roundRect()` は使用禁止（iOS実績のため）。既知の例外は `ctx.ellipse?0:0` の存在確認三項1箇所のみ。
6. コード検索は必ず base64 を除外する（`grep -iv base64` またはツール側の data-URI 除去）。20万字のbase64が偶然一致を量産する。
7. **出力・成果物のファイル名は半角英数字のみ。** 日本語・全角入りはiOS Safariで「読み込めませんでした」になる実績。
8. **同一セレクタのCSSは後勝ち。** 特に base64 の後ろに隠れたプロパティに注意。CSSを足す前にセレクタ名で全出現を確認する。
   【根拠】`.home-logo` が2箇所にあり、base64 の後ろの `animation` が先の指定を上書き。`logoFloat` が一度も動いていなかった。
9. **変数の定義順に依存する変更は宣言順も確認する。** `var` は巻き上がるので構文エラーにならず `undefined` で静かに壊れる。
   【根拠】v232: `lvW`→`LEN_K`→`GOAL_X` の順に並べ替えて解決。
10. **常時rAFループの上に不透明画面を重ねるときは下層の描画停止を検討。** `draw()` は時刻参照0件の純粋関数なので、状態が変わらない限りスキップは無損失（`_drawSig` 方式）。ただし状態変化直後の1フレームは必ず描く。
11. ** ダメージは必ず `takeDamage()` を通す。** `p.hp -= n` の直接減算はスキルのクッション処理を全部バイパスする（過去に落下ダメージで実際に発生）。新しいダメージ源を足すときは必ず grep で直接減算がないか確認。
12. **スクロールする画面のボタンで `touchstart` + `preventDefault()` をしない。**
   iOSはそれでスクロール操作そのものが死ぬ。さらに「ドラッグしただけでボタンが発火」する。
   タップは「指の移動が8px未満のときだけ touchend で発火」にする（`_csTap` / `showStageMap` の方式）。
   【根拠】v276: キャラ選択を1枚絵＋透明ボタンにしたら当たり判定が画面のほぼ全面を覆い、
   指の置き場が消えて完全にスクロール不能になった。実測A/Bでドラッグ 0px→283px。
   同じ書き方は以前からあったが、当たり判定が小さいうちは「余白を掴めば動く」ので露見しなかった。
13. **iOSの色文字列**：`rgb(...)` を `shadeCol` に二重で通すと `rgb(NaN,...)` になり iOS Safari の `addColorStop` が**無言で**落ちる。色加工の入口で形式を確認する。

## 3. コミット前ゲート（全部通ってからコミット）

```bash
node tools/check.js game/my_hero.html   # 下の 1〜3 を機械照合
node tools/render_bg.js                 # 背景を触った場合のみ必須
```

1. **acorn構文チェック**（ecmaVersion 2020）全 script ブロック OK。Node の `new Function` は IIFE + strict で偽エラーを出すので**信用しない**。
2. `__dbg` 0件。
3. 禁止API 0件（`ctx.ellipse(` / `ctx.roundRect`。存在確認三項は既知の例外として自動除外）。
4. **撤去の完全性**：機能を撤去したコミットでは、関連識別子すべての grep が0件（base64内の偶然一致を除く）。
5. **差分報告**：コミットメッセージまたはユーザー報告に、サイズ増減と理由／追加・変更した識別子一覧／**未検証項目と実機チェックリスト**を明記。
6. **未検証リストの持ち越し**：実機で確認できていない項目は `docs/SPEC.md` §未検証 に必ず追記し、消化されるまで消さない。静的検証だけで「完了」にしない。

## 4. アセットの可逆性

1. 画像などのバイナリを加工するときは、**加工前の base64 が git 履歴に残っていることを確認してから**行う。
2. 復元は旧コミットの base64 を**そのまま**書き戻す（再圧縮禁止）。ハッシュ照合で一致を確認する。
   【根拠】v216〜218のロゴ加工不採用時、v215のbase64を無劣化復元しハッシュで証明できた。

## 5. フィードバック対応

1. 「できてない」「おかしい」「重い」が来たら**まず自分の実装を疑う**。実測 → 原因特定 → 報告 → 修正。原因不明のまま修正案を出さない。
   【根拠】v225→227:「重い」の一言から、ホーム悪化は自分のせい／プレイ中の重さは元から、と切り分けた。
2. **実機スクショは一次データ**。届いたら数値化（要素位置・輝度・色）してから解釈する。§7 の環境定数も更新する。
3. **同じ方向の修正が2回連続不採用なら、3回目ではなく方針転換を提案する。**
4. 不採用機能の撤去は「残骸ゼロ」（§3-4を適用）。
5. **候補は複数レンダリングして数値比較してから1案を選ぶ**。目視だけで選ばない。
6. **1回の検証で変える変数は1つだけ。** 特に「AIプロンプト」「素材画像」「コード」は別変数として扱う。
   【根拠】v234c: 白背景バグ検証で3変数を同時に変え、原因を特定できず出荷内容が決められなかった。
7. **ユーザーの体感表現を比喩と決めつけない。**「焦点が合わない」は文字通り解像度不足だった。

## 6. ドキュメントの鮮度

1. コミットするたびに `docs/SPEC.md` の該当箇所（現状・未検証・パラメータ表）を**同じコミットで**更新する。SPEC だけ古い状態を作らない。
2. SPEC・コメント・報告のどこにも行番号を書かない（§0-4）。
3. `docs/LEGACY_hikitsugi_v234c.md` は**読み取り専用のアーカイブ**。更新しない。中の行番号はv234時点のもので現ファイルとは一致しない。

## 7. 実機環境定数（スクショ実測・変わったら更新）

- 主要ターゲット：Instagram内蔵ブラウザ（WKWebView）。
  スクショ実寸 1170×2532 @3x ／ IGクローム509px → **コンテンツ 390×674 CSS px**（2026-07-25実測）
- 併せて必ず対応：iPhone SE ≈ **375×548**（現在ボタンがはみ出し中。SPEC §既知バグ参照）
- canvas：論理 390×390。内部解像度は `390×CV_K`（現在 CV_K=1.25 → 488）。**ゲーム中キャラの実データ量は46×75px**。これより細かい作り込みは画面に出ない。
- 音声（`sndInit`）はユーザー操作起点でのみ初期化可能（iOS制約）。**setTimeout の中で呼ぶと解錠されない**。
- ヘッドレス確認：Playwright は `file://` で開き、localStorage に `yk_seen_story=1` を入れてイントロをスキップ。`page.evaluate` からIIFE内の変数には触れない → 検証はスタンドアロンのテストハーネスHTMLで行う。
- 性能測定の型：CPUスロットル6倍で rAF のフレーム間隔（fps・p95・25ms超数）。要素別A/Bは `addStyleTag` で `display:none` を注入して比較。見た目の比較はスクショの数値化（領域別平均輝度・画像差分）。
- 実機の直近実測：プレイ中 32.8fps／ホーム 42.8fps（CPU6倍・v227時点）。**プレイ中に余剰予算はほぼ無い**。

## 8. git 運用

1. ブランチは main 一本でよい（一人開発・1コミット1テーマが守られている限り）。実験は branch を切り、不採用なら merge せず削除。
   **Claude Code リモートセッションの場合は例外**：作業ブランチ（`claude/...`）で開発し、PRを作り、マージはユーザーが行う（§9-2の承認制と対応）。
2. **PRをマージしたら、マージ結果に狙いのコミットが入っているか必ず確認してから「完了」と言う。**
   GitHubのマージは、PR作成時点のhead SHAで実行されることがある。**push直後にマージすると
   後から積んだコミットが取り込まれない。** マージ後は必ず `git fetch origin main` して
   `git log --oneline origin/main` と、狙いの識別子の `grep -c` で中身を照合する。
   取りこぼしていたら、ブランチを消さずに（リモートには残っている）新しいPRを立て直す。
   【根拠】v246→#9・v248→#11・v250〜v256→#12 と**3回連続で取りこぼした**。
   3回とも「マージ完了」とユーザーに報告した後で発覚した。
   **v280〜v283→#25 で4回目**。PRの説明文を更新しただけで照合を省いたのが原因。
   ⚠ **PRを作ったあとに追加でpushしたら、そのPRはもう信用しない。**
   説明文の更新では取りこぼしは防げない（GitHubはPR作成時点のheadでマージすることがある）。
   追加pushをしたら、次にユーザーへ渡す前に必ず
   `git log --oneline origin/main..HEAD` が**空であること**を確認する。
   空でなければ、そのコミットは届いていない＝新しいPRを立て直す。
3. バージョンを進めるコミットに `vNNN` タグ。ユーザーへ渡す実機確認用ファイルはタグ時点の `game/my_hero.html` をそのままコピーし、ファイル名は `my_hero_vNNN.html`（半角英数字）。
4. `tools/check.js` が落ちる状態をコミットしない。
5. base64 を含む巨大diffになるコミット（画像差し替え）は、メッセージに**何の画像を・なぜ・元に戻す方法**を書く。
6. **公開URLの正体はルート直下の `index.html`**（GitHub Pages・main ブランチ）。開発の本体は `game/my_hero.html` で、リリース時にルート `index.html` へそのままコピーして公開する。片方だけ更新して乖離させない。

## 10. UIデザインの方針（ユーザー指定・§1やSPECより優先する上位方針）

**大前提：My HERO はスマホゲーム。ゲーム本体の処理・動作・レスポンス・データ容量・保守性が最優先。
見た目のためにそれを削らない。豪華さではなく「分かりやすさ」と「操作したくなる楽しさ」を取る。**

目指す方向：「シンプルだけど楽しい」「ポップだけど見やすい」「子供が触りたくなる」「任天堂系の直感的な楽しさ」。

### 10-1. やらないこと（厳守）
- 既存のゲーム機能を壊す変更
- ゲーム本体の処理性能を犠牲にする装飾
- 不要な画像素材の大量追加／背景画像の重ね掛け
- 不要なアニメーション・エフェクトの追加
- **同じ意味の装飾を複数重ねる**（例：同じボタンに鼓動＋光沢＋発光）
- スマホでの表示速度低下・データ容量の不必要な増加
- コードの不必要な複雑化

### 10-2. 実装前に必ず自問する2点
1. **この変更はゲーム全体にとって本当に必要か？**
2. **もっと軽い方法で同じ効果を出せないか？**

### 10-3. 装飾を足すときの手順
1. その画面で**既に走っている装飾を数える**（`document.getAnimations()` の本数と対象）。
   同じ対象・同じ意味のものが既にあるなら、**足すのではなく置き換える**。
2. 絵（イラスト）に既に描かれている表現を、CSS/DOMで**二重に足さない**。
3. 追加後、`getAnimations()` の本数・ファイルサイズ・load時間を実測して報告に載せる。

### 10-4. 品質が下がりうる変更は勝手に実装しない
デザイン改善でゲーム本体の品質が下がる可能性がある場合は、**実装前に影響と代替案を報告して指示を仰ぐ**。
「見た目を良くするためなら何でも追加する」という判断はしない。

【根拠】v270〜v274 で見た目を優先した結果、ホーム画面に**常時11本**のアニメが走り
（うち6本は v270 で追加した `homeShine`、しかも `ctaPulse` と同じボタンに重複）、
ファイルは 1.38MB → 1.65MB（+22%）に増えた。ユーザーから方針の明文化を指示された。

## 9. ユーザー協働ルール（ユーザー指定の12ルールの統合）

§0〜8 と重複しない要点のみ。重複するものは各節が正（1テーマ=§0-2、復元可能=§4、ドキュメント鮮度=§6 など）。

1. **推測で修正しない。** 必ず実際のコードを確認してから判断する。
2. **承認制。** 変更前に「現状・原因・修正方針・変更ファイル・影響範囲・リスク・確認方法」の7項目を説明し、ユーザーの承認を待ってから編集する。
3. **勝手に削除しない。** 不要・重複・古いと思われるファイルも、承認なしで削除しない。
4. **必要な範囲だけ読む。** 全ファイル一括スキャンやHTML全文の一括読み込みをしない（セッション消費を抑える）。
5. **最小差分。** 依頼されていない見た目・操作感・バランス・UI・仕様を変えない。
6. **小出しにしない。** 必要な内容は最初から提案・生成物に統合し、ユーザーに何度も追加入力させない。
7. **初心者対応。** ユーザー操作が必要なときは「何を・どこで・なぜ」を具体的に書く（実機確認依頼の書式は §0-3 参照）。
8. **成果物はコードでなく「開くだけのURL」で渡す。** チャットにHTMLやコードを貼らない。
   リモートセッションなら push→PR→マージ後の公開URL（§8-5）、単発の確認物なら実機で直接開けるリンク。
   ユーザーは iPhone だけで作業しているので、貼られたコードを保存してURL化する手段がない。
   コードを見せるのは「原因の説明で数行だけ引用する」場合に限る。
   【根拠】v246: 実機確認をコード貼り付けで依頼し、ユーザー側に「貼ってURL化して開く」手作業が発生した。
9. **URLはコードブロック（``` や `）に入れず、地の文にそのまま書く。**
   コードブロックに入れるとスマホでは「コード」枠として表示されてタップできず、
   横幅で末尾が見切れてクエリ（`?test=1&v=246` など）が読めなくなる。
   【根拠】v246: URLを ``` で囲って渡したら、実機で「どう見てもコードやけど」と指摘された。
   §9-8 を守ったつもりで、渡し方が同じ問題を再発させていた。
10. **実機確認をお願いする回では、必ず同じ返信の中にリンクを貼る。**
   「マージしてくれたらリンク渡すわ」で終わらせない。ユーザーに催促の1往復をさせた時点で失敗。
   マージ前でも、まず `git fetch origin main` でマージ済みか自分で確認する（たいてい済んでいる）。
   本当に未マージなら「今マージしたらこのURLで見られる」と**URLを先に書いてから**マージを依頼する。
   §8-2 の照合と公開ページのバイト比較は、リンクを貼る前に自分で終わらせておくこと。
   【根拠】v270: 4件の修正を報告したのにリンクを貼らずマージ依頼だけして、
   「実機確認の際はリンク貼り付けて！って。もう完全記憶してその流れは」と指摘された。
   確認しに行ったらPRは既にマージ済み・デプロイも完了していて、渡さない理由が最初から無かった。
   **v285でも同じことをして「結構忘れがちやな」と再指摘された（2回目）。**
   記憶に頼るのをやめ、**リンクを渡す前に必ず `sh tools/relink.sh` を実行する**。
   このスクリプトが ①`origin/main..HEAD` が空 ②公開/main/手元のバイト一致 を機械判定し、
   通ったときだけ貼るべきURLを出力する。🔴 が出たら貼らずに原因を直す。
   「マージしてくれたらリンク渡すわ」で終わらせるのは、この手順を飛ばした状態と同じ。
