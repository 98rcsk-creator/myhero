# 変更履歴

新しいものが上に来るように書きます。

書き方の目安:
- **日付** / 何をしたか / なぜしたか / 影響範囲

---

## 2026-08-13 — v237

### 星の条件をクリア画面に表示（SPEC §9-5 案A）

- **何を**: `showStageClear` に「ひょうか NN%　★Nは XX%から」と、次の星までの
  具体的なヒント（「てきを あと N たおすと ★N！」等）を追加
- **なぜ**: 調査（SPEC §9）で、星を決める合算スコアが画面に出ておらず、
  「敵を全滅させたのに★2」の理由（残HPが25%の重みを持つ）が見えないと判明したため
- **判定式は一切変更していない** → ゲームバランスは不変
- **変更量**: +21行 / 削除0（`game/my_hero.html` と公開用 `index.html` の2ファイル）
- **追加した識別子**: `_starHint` / `_tgtStar` / `_gap` / `_restE` / `_restC` /
  `_needE` / `_needC` / `_tip`（いずれも実装前に grep 0件を確認）
- **検証**: `tools/check.js` 全ゲート合格（両ファイル）。
  IG実測ビューポート390×674で4ケース描画確認（★3/★2/★2/★1）、JSエラー0件、
  ヒント文の残数が実際と一致することを確認
- **未検証**: 実機確認は未了（SPEC §6 v237 の項目参照）。
  クリア画面の内容高が 683px→742px（+59px）に増加

---

## 2026-08-13

### Claude Code 開発体制へ移行（リポジトリ再構成）＋ ゲームを v236 に更新

- **何を**:
  - Claude チャット時代の成果物一式（zip）をリポジトリに導入
    - `CLAUDE.md`（開発ルールブック・唯一の正本）
    - `docs/SPEC.md`（仕様・現状・地雷・未検証・ロードマップの正本。旧SPECを差し替え）
    - `docs/LEGACY_hikitsugi_v234c.md`（旧引継書アーカイブ・読み取り専用）
    - `game/my_hero.html`（**v236**・開発本体。8,125行 / 1,049,835B）
    - `tools/check.js` / `tools/render_bg.js`（出荷前ゲート・背景計測）
  - ルート `index.html` を v236 に更新（公開用コピー。`game/` と同一内容）
  - ルート `README.md` を開発リポジトリ用に刷新
  - `CLAUDE.md` にユーザー協働ルール（12ルールの要点）を §9 として統合
- **削除（承認済み）**:
  - `my_hero_confirm_modal_fixed.html`（v236に修正内容が含まれ役目終了。Git履歴に残存）
  - `docs/RULES.md`（CLAUDE.md §9 に統合）
  - `docs/README.md`（ルート README.md に統合）
- **なぜ**: 開発を Claude Code に一本化するにあたり、ルール・仕様・ツールの正本を
  リポジトリ内に置き、二重管理をなくすため（CLAUDE.md §0-5）
- **検証**: `tools/check.js` 全ゲート合格（構文 / __dbg 0件 / 禁止API 0件）を
  `game/my_hero.html`・`index.html` の両方で確認
- **未検証**: v236 の実機確認は未了（`docs/SPEC.md` §6 のチェックリスト参照）

---

## 2026-08-07

### docs/ フォルダを新設

- **何を**: `docs/` に6つのドキュメントを作成（README / SPEC / CHANGELOG / TODO / DEPLOY / RULES）
- **なぜ**: 仕様・履歴・アイデアを書き残す場所がなかったため
- **影響**: ゲーム本体への影響なし（`index.html` は未変更）

### 公開版 index.html を最新版に更新

- **何を**: 手元の最新版（1,030,489 バイト）を `index.html` に反映
- **なぜ**: 公開ページが 2026-07-09 の古い版のままだったため
- **変更量**: +820行 / -216行
- **主な内容**:
  - マイヒーロー削除確認を、ブラウザ標準 `confirm()` から独自モーダル（`mhDeleteModal`）へ変更
  - オーバーレイのスクロール設定を修正（`ov.classList.add('scrollable')`）
  - タイトル画面の刷新（「▶ タップしてスタート ◀」ボタン追加、メニュー拡張）
  - その他機能追加
- **確認**: iPhone相当（390×844）で起動確認・JSエラー0件
- **PR**: [#1](https://github.com/98rcsk-creator/myhero/pull/1)（マージ済み）

#### なぜ削除モーダルの変更が重要だったか

ブラウザ標準の `confirm()` は、Instagram 内ブラウザなど一部の環境で
表示が抑制されることがあります。その場合「確認なしで即削除」となり、
大切に育てたマイヒーローを誤って消してしまう危険がありました。
独自モーダルにしたことで、どの環境でも確実に確認画面が出るようになりました。

---

## それ以前（GitHub の履歴より）

| 日付 | 内容 |
|------|------|
| 2026-07-11 | `my_hero_confirm_modal_fixed.html` を追加（削除モーダル修正版） |
| 2026-07-09 | `index.html` 更新 |
| （初期） | `index.html` 追加 |
| （初期） | リポジトリ作成（README.md） |

> このころは「ファイルを直接アップロード」で開発が進んでいたため、
> 詳細な変更内容は記録が残っていません。
> 今後はこのファイルに記録していきます。
